set-pp-renderer ascii
set-pp-width 80

set-pp-type     Omit
-- set-pp-type     Show

-- set-pp-coercion Kind
set-pp-coercion Show
-- set-pp-coercion Omit

---- Messing about

-- -- rhs-of 'abst1
-- occurrence-of 'abst

-- binding-of 'trip1 ; nonrec-rhs
-- abst-repr
-- -- [app-fun,app-fun,app-fun,app-fun,app-fun]

-- unfold
-- standardize-case

-- rhs-of 'sumVI1
-- unfold
-- lam-body
-- standardize-case
-- bash

-- TODO: replace bash with simpler steps:
-- case-float-case, inline-worker, case-reduce

rhs-of 'sumT4

-- standardize-con
-- bash

-- rhs-of 'sumV1'
-- -- unfold

-- bash-all

-- one-td standardize-case
-- one-td unfold-nonprim

-- one-td (standardize-case <+ unfold-nonprim)

-- repeat (one-td (standardize-case <+ unfold-nonprim <+ cast-float-app)) -- >>> try bash-all

-- any-td (repeat (standardize-case <+ unfold-nonprim <+ cast-float-apps <+ cast-float-lam)) >>> try bash-all

any-td (repeat (standardize-case <+ unfold-nonprim))
try (repeat (any-bu cast-float))
try simplify

try unshadow
