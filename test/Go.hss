set-pp-renderer ascii
set-pp-width 80

set-pp-type     Omit
-- set-pp-type     Show

-- set-pp-coercion Kind
set-pp-coercion Show
-- set-pp-coercion Omit

--------

rhs-of 'sumT2

any-td (repeat (standardize-case <+ standardize-con <+ unfold-nonprim))
try (repeat (any-bu cast-float))
try simplify

try unshadow
